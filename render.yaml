services:
  - type: web
    name: future-cart-dashboard
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile.streamlit
    dockerCommand: streamlit run src/dashboard/app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
